# Backend Component

Este es el directorio del servicio backend (Node.js/Spring Boot) de **GlobalFin Services**.
Su pipeline se ejecuta selectivamente bajo cambios en esta carpeta y corre pruebas utilizando una matriz de sistemas operativos (Ubuntu, Windows) y runtimes (Node 18, 20).
